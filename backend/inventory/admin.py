from django.contrib import admin
# from .models import InventoryItem

# Register your models here.

# Example: InventoryItem Admin
# Uncomment and modify as needed
#
# @admin.register(InventoryItem)
# class InventoryItemAdmin(admin.ModelAdmin):
#     """
#     Admin interface for InventoryItem model.
#     """
#     list_display = ['name', 'quantity', 'unit', 'category', 'location', 'created_at']
#     list_filter = ['category', 'location', 'created_at']
#     search_fields = ['name', 'description']
#     readonly_fields = ['created_at', 'updated_at']
#     ordering = ['-created_at']
#     
#     # Optional: Add filters on the right sidebar
#     # date_hierarchy = 'created_at'
